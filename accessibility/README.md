# Accessibility ownership

Accessibility is a release gate, not a later enhancement. See `docs/accessibility.md` for required manual checks. Any new interactive component must document keyboard behavior, focus movement, state attributes, and reduced-motion behavior before merge.
